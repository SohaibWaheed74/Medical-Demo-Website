## Packages
framer-motion | For beautiful page entrance animations and interactions
react-hook-form | For robust form handling in appointment and contact sections
@hookform/resolvers | For Zod schema validation with react-hook-form
clsx | For conditional class merging (utility for UI components)
tailwind-merge | For safe Tailwind class merging (utility for UI components)
date-fns | For date manipulation and formatting

## Notes
Static images (logo, hero) will be sourced from Unsplash.
API endpoints match shared/routes.ts exactly.
Theme uses CSS variables for runtime dynamic styling support if needed.
